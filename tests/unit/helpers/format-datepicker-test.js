
import { formatDatepicker } from 'holiday-list/helpers/format-datepicker';
import { module, test } from 'qunit';

module('Unit | Helper | format datepicker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDatepicker([42]);
  assert.ok(result);
});

