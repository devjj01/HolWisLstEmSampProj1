
import { checkEdit } from 'holiday-list/helpers/check-edit';
import { module, test } from 'qunit';

module('Unit | Helper | check edit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkEdit([42]);
  assert.ok(result);
});

